# Copyright Microsoft Corporation

set(TARGET rtosmon_init.test)

set(SOURCES
    test_rtosmon_init.cpp
    ${CMAKE_SOURCE_DIR}/src/libs/runtime_init/rtosmon_init.c
)

# Compile test
add_executable(${TARGET} ${SOURCES})

target_include_directories(
    ${TARGET} PUBLIC
        ${CMAKE_CURRENT_LIST_DIR}
)

target_link_libraries(${TARGET}
    PRIVATE
        fwlib.lib.init
        fwlib.lib.utils
        fwlib.lib.status
        fwlib.lib.rtosmon
        ms::cmocka::testrunner
        ms::lib::register_headers
        silibs::idsw_kng
        silibs::platform
        ms::cmsis::interface
        ms::lib::utils
        ms::lib::systick_update
        ms::lib::threadx
        ms::service::m7_rtosmon
)

set_target_properties(${TARGET} PROPERTIES FPFW_PLATFORM_IMPL_ATOMIC "ms::lib::atomics")
set_target_properties(${TARGET} PROPERTIES FPFW_PLATFORM_IMPL_INTERRUPTS "ms::lib::interrupts")

lint_target(${TARGET})
format_target(${TARGET})
coverage_target(${TARGET})
unit_test_target(${TARGET})

#mock the functions needed
mock_target(${TARGET}
    crash_dump_bug_check
    get_timestamp_counter_freq
    idsw_get_cpu_type
    rtosmon_init
    sdm_get_system_timestamp
    systick_get_emcpu_clock
    _tx_time_get
)
