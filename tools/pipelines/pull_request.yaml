
name: 0.1.2.$(rev:r)

trigger: none

parameters:
# One Branch Parameter
- name: 'debug'
  displayName: 'Enable debug output'
  type: boolean
  default: false

variables:
- group: ef-pipeline-config
# One Branch Variables
- name: system.debug
  value: ${{ parameters.debug }}
- name: WindowsContainerImage
  value: 'onebranch.azurecr.io/windows/ltsc2019/vse2019:latest'

resources:
  repositories: 
    - repository: templates
      type: git
      name: OneBranch.Pipelines/GovernedTemplates
      ref: refs/heads/main

extends:
  template: v2/OneBranch.NonOfficial.CrossPlat.yml@templates
  parameters:
    cloudvault: # https://aka.ms/obpipelines/cloudvault
      enabled: false
    globalSdl: # https://aka.ms/obpipelines/sdl
      asyncSdl:
        enabled: true
        tsaOptionsFile: .config/tsaoptions.json
      tsa:
        enabled: false # onebranch publish all sdl results to TSA. If TSA is disabled all SDL tools will forced into 'break' build mode.
      # credscan:
      #   suppressionsFile: $(Build.SourcesDirectory)\.config\CredScanSuppressions.json
      binskim:
        break: true # always break the build on binskim issues in addition to TSA upload
      policheck:
        break: true # always break the build on policheck issues. You can disable it by setting to 'false'
      # suppression:
      #   suppressionFile: $(Build.SourcesDirectory)\.gdn\global.gdnsuppress
      # Code QL not supported:
      #  ##[warning]CodeQL analysis is not available on 'pull', 'tags', 'user(s)' and 'topic' branches, or OneBranch Pull Request pipelines. This cannot be overridden.
      codeql:
        compiled: 
          enabled: false

    # Build stage
    stages:
    - stage: Build_x86_Debug
      dependsOn: []
      jobs:
      - template: tools/pipelines/templates/build.yaml@self
        parameters:
          Toolchain: i386-pc-windows-msvc
          Configuration: Debug
          ArtifactName: Build_x86_Debug

    - stage: Build_Arm_EABI_AARCH_Debug
      dependsOn: []
      jobs:
      - template: tools/pipelines/templates/build.yaml@self
        parameters:
          Toolchain: arm-eabi-aarch
          Configuration: Debug
          ArtifactName: Build_Arm_EABI_AARCH_Debug

    - stage: SVP_Pythia_Tests
      dependsOn: [Build_Arm_EABI_AARCH_Debug]
      jobs:
      - template: tools/pipelines/templates/svp_pythia_test.yaml@self
        parameters:
          ArtifactName: SVP_Pythia_Tests

    - stage: Unit_Test_x86
      dependsOn: [Build_x86_Debug, Build_Arm_EABI_AARCH_Debug]
      jobs:
      - template: tools/pipelines/templates/unittest.yaml@self
        parameters:
          Toolchain: i386-pc-windows-msvc
          Configuration: Debug
          # Pipeline variables defined within ADO
          BranchCoverage: $(branch_coverage)
          LineCoverage: $(line_coverage)
          ArtifactName: Unit_Test_x86


