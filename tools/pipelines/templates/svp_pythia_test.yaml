parameters:
- name: Toolchain
  type: string
  default: 'arm-eabi-aarch'
- name: Configuration
  type: string
  default: 'Debug'
- name: ArtifactName
  type: string
  default: 'bins'
- name: Pool
  type: string
  default: '1pfw-kingsgate-windows'
- name: TestPlan
  type: string
  default: '.\tests\functional\pythia\test_plan\svp_single_die_ci_test_plan.args'
- name: build_id
  type: string
  default: '0'
- name : ifwi
  type: string
  default: 'kingsgate.ifwi.svp.debug.custom.dat'
- name : TimeoutInMinutes
  type: number
  default: 240

jobs:
- job: 'SVP_Pythia_Tests'
  timeoutInMinutes: ${{ parameters.TimeoutInMinutes }}
  displayName: SVP Pythia Tests
  pool: 
    name: ${{ parameters.Pool }}
    isCustom: true
    type: windows

  variables:
    ob_outputDirectory: '$(Build.SourcesDirectory)\.testlogs\pythia'
    ob_artifactSuffix: '$(System.JobAttempt)'

  steps: 

    # Set up permissions for other repos
    - template: auth.yaml

    # Dump agent information (useful for debugging)
    - template: agent_info.yaml

    # Download input artifact for CI & nightly run
    - task: DownloadPipelineArtifact@2
      condition: and(succeeded(), eq('${{ parameters.build_id }}', '0'))
      inputs:
          buildType: 'current'
          downloadType: 'single'
          artifactName: 'drop_Build_Arm_EABI_AARCH_Debug_Build'
          downloadPath: '$(System.ArtifactsDirectory)/bins.arm-eabi-aarch.Debug'

    # Download input artifact for nightlystress and weekendstress
    - task: DownloadPipelineArtifact@2
      condition: and(succeeded(), ne('${{ parameters.build_id }}', '0'))
      inputs:
          buildType: 'specific'
          project: '2610da6a-0168-48f8-b34a-f1c882cb6660'  # Woodinville
          definition: '8678'  # Kingsgate.MSCP-Official
          buildVersionToDownload: 'specific'
          buildId: ${{ parameters.build_id}}
          artifactName: 'drop_Build_Arm_EABI_AARCH_Debug_Build'
          downloadPath: '$(System.ArtifactsDirectory)/bins.arm-eabi-aarch.Debug'

    # 1. Setup the environment
    # 2. Invoke Pythia
    # Logs will be stored under $(Build.SourcesDirectory)\.testlogs\pythia
    - task: PowerShell@2
      displayName: Run Test Suites
      env:
        AZURE_DEVOPS_EXT_PAT : $(System.AccessToken)
      inputs:
        workingDirectory: '$(Build.SourcesDirectory)'
        targetType: 'inline'
        showWarnings: true
        script: | 
          try {
            ./start.ps1 -Toolchain ${{ parameters.Toolchain }} -Configuration ${{ parameters.Configuration }}

            $bin_src_dir = "$(System.ArtifactsDirectory)/bins.arm-eabi-aarch.Debug/bin"
            $bin_dest_dir = "${env:REPO_APP_BUILD_DIR}/${env:REPO_APP_BUILD_CONFIG}/arm-eabi-aarch"
            Copy-Item $bin_src_dir $bin_dest_dir -Recurse -Force

            # Run the test suite
            Invoke-Pythia -test ${{ parameters.TestPlan }} -platform svp -ifwi ${{ parameters.ifwi }}

            # Remove intermediate output / output that contains system variables (ie: Azure credentials)
            Remove-Item "$(Build.SourcesDirectory)\.testlogs\pythia\**\output.xml"
            Remove-Item "$(Build.SourcesDirectory)\.testlogs\pythia\**\rlog.txt"
            Remove-Item "$(Build.SourcesDirectory)\.testlogs\pythia\**\generated" -Recurse -Force
            Remove-Item .externs -Force -Recurse
          }
          catch {
            Write-Error "An error occurred during Pythia Tests execution: $_"
            exit 1
          }

    # Publish the results of the test suite
    - task: PublishTestResults@2
      displayName: 'Publish: SVP Pythia Test Results'
      condition: succeededOrFailed()
      inputs:
        testRunTitle: 'Kingsgate SVP Pythia Tests'
        testResultsFormat: 'JUnit'
        testResultsFiles: '**\Results.xml'
        searchFolder: '$(Build.SourcesDirectory)\.testlogs\pythia'
        failTaskOnFailedTests: false
        publishRunAttachments: true
        mergeTestResults: true

    - task: PowerShell@2
      displayName: 'Prepare Pythia Logs for Publishing'
      condition: always()
      inputs:
        targetType: 'inline'
        script: |
          $source = "$(Build.SourcesDirectory)\.testlogs\pythia"
          $target = "$(Build.ArtifactStagingDirectory)\pythia_clean"
          New-Item -ItemType Directory -Force -Path $target

          Get-ChildItem -Path $source -Recurse | ForEach-Object {
            try {
              $relativePath = $_.FullName.Substring($source.Length)
              $destPath = Join-Path -Path $target -ChildPath $relativePath
              $destDir = Split-Path -Path $destPath -Parent
              New-Item -ItemType Directory -Force -Path $destDir | Out-Null

              $stream = [System.IO.File]::Open($_.FullName, 'Open', 'Read', 'None')
              $stream.Close()
              Copy-Item $_.FullName -Destination $destPath -Force
            } catch {
              Write-Host "Skipping locked file: $($_.FullName)"
            }
          }

    # Publish results as a pipeline artifact
    - task: PublishPipelineArtifact@1
      displayName: 'Publish: Pipeline Artifacts'
      condition: always()
      inputs:
        targetPath: '$(Build.ArtifactStagingDirectory)\pythia_clean'
        artifact: 'drop_${{ parameters.ArtifactName }}_SVP_Pythia_Tests$(System.JobAttempt)'
        publishLocation: 'pipeline'
        parallel: true
